{% extends 'base.html.twig' %}

{% block body %}

<form class="" action="{{path('security_login')}}" method="post">
  <div class="form-group">
    <input type="text" class="form-control" required name="_username" placeholder="Votre pseudo ...">
  </div>
  <div class="form-group">
    <input type="password" class="form-control" required name="_password" placeholder="Votre mot de passe ..." name="" value="">
  </div>
  <div class="form-group">
    <button type="submit" class="btn btn-success">Valider!</button>
  </div>
</form>
<a href="{{path('security_registration')}}">Créer un compte utilisateur</a>

{% endblock %}
